UBUNTU
cat /etc/os-release - returns information on OS installed on a device
locale - returns info on configured language settings
hostname - returns the name which was set on a PC, Not FQDN (for LQDN need to add flag -f)
who -b - shows the logged user. 
dmidecode | grep -A3 '^System Information' - DMI - special tool which allows to return info on a hardware of PC.
ifconfig - utility which allows to manage network interface configurations
cat /proc/version - returns version of Ubuntu and core installedm date when it was installed
ip route | grep default - returns the default ip route from ip route table

SUSE
cat /etc/os-release
locale
hostname
who -b
ifconfig
uname -m
cat /sys/devices/virtual/dmi/id/sys_vendor
cat /sys/devices/virtual/dmi/id/product_name

REDHAT/Oracle 7 or greater
cat /etc/os-release
lsb_release -a
locale
hostname
who -b
dmidecode | grep -A3 '^System Information'
ip addr show
cat /proc/version

REDHAT/Oracle 6
lsb_release -a
locale
hostname
who -b
dmidecode | grep -A3 '^System Information'
ip addr show
cat /proc/version

AmazonLinux (AMI)
cat /etc/os-release
locale
hostname
who -b
ifconfig
cat /proc/version
cat /sys/devices/virtual/dmi/id/sys_vendor
cat /sys/devices/virtual/dmi/id/product_name

Generic Linux (Fallback)
cat /etc/os-release
locale
hostname
who -b
ifconfig
cat /proc/version
cat /sys/devices/virtual/dmi/id/sys_vendor
cat /sys/devices/virtual/dmi/id/product_name

All Linux
cat /etc/verismic - returns the device ID of a device
df - report file system disk space usage
echo xxxxxx | sudo tee /etc/verismic
shutdown -r now (for reboot tasks) - reboots the device
who -b
